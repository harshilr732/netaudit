{
  "_comment": "Copy to credentials.json (GITIGNORE that copy). Secrets are NEVER stored here - 'secret_env' names an environment variable that holds the real password. Rules are matched top-to-bottom; put more specific networks first.",

  "rules": [
    {
      "network": "192.168.1.50/32",
      "transport": "winrm",
      "username": "DOMAIN\\svc-audit",
      "domain": "DOMAIN",
      "winrm_auth": "ntlm",
      "use_tls": false,
      "secret_env": "AUDIT_WIN_PW"
    },
    {
      "network": "10.0.20.0/24",
      "transport": "ssh",
      "username": "svc-audit",
      "ssh_key_path": "C:\\Users\\you\\.ssh\\audit_ed25519"
    }
  ]
}
